# v1.0.1

Polished public ISO installer release.

- Uses a fully automatic per-user install under `%LOCALAPPDATA%\WindowsSafeRebootGuardian` with no UAC prompt.
- Adds `fix.ps1` toolkit and `install.ps1` installer to the ISO.
- Documents and verifies a PowerShell one-liner that downloads and installs the latest release ISO.
